[project]
name = "pdf-2-anki"
version = "0.1.0"
description = "PDF to Anki flashcard generator using LangChain and Ollama"
requires-python = ">=3.11"
dependencies = [
    "langchain-ollama",
    "pymupdf",
    "pillow",
]

[tool.uv]
dev-dependencies = []
